// Question 1:
const add = (a, b) => {
    return a + b;
}
module.exports = add;

// Question 3:
// To both add and multiply together, use the add function as a parameter
// within the multiply function:

// multiply(2, add(4,4));
